<%= form_for(@login) do |f| %>
  <% if @login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-md-4 col-sm-6 col-xs-12">
    <div class="form-group">
      <%= f.label :estado %><br>
      <%= f.number_field :estado, class: 'form-control'  %>
    </div>
  </div>  
  <div class="col-md-4 col-sm-6 col-xs-12">  
    <div class="form-group">
      <%= f.label :sesion %><br>
      <%= f.text_field :sesion, class: 'form-control'  %>
    </div>
  </div>  
  <div class="col-md-4 col-sm-6 col-xs-12">  
    <div class="form-group">
      <%= f.label :clase_usuario %><br>
      <%= f.number_field :clase_usuario, class: 'form-control'  %>
    </div>
  </div>  
    <div class="form-group">
     <%= f.submit 'Guardar', class: 'btn btn-primary' %> 
    </div>
</div>

<% end %>
